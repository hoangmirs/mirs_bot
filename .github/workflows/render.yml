name: Render Awake

on:
  schedule:
    - cron: "*/14 * * * *"

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: jtalk/url-health-check-action@v3
        with:
          url: ${{ secrets.WEBHOOK_HOST }}
